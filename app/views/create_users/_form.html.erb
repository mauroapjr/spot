<%= form_with(model: create_user) do |form| %>
  <% if create_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(create_user.errors.count, "error") %> prohibited this create_user from being saved:</h2>

      <ul>
        <% create_user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :password_digest %>
    <%= form.text_field :password_digest %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
